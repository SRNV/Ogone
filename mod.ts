import Ogone from "./classes/main/index.ts";
import { OgoneConfiguration } from "./.d.ts";
import * as l from "./.d.ts";
import { Configuration } from "./classes/config/index.ts";

export default {
  async run(opts: OgoneConfiguration): Promise<void> {
    Configuration.setConfig(opts);
    await new Ogone(opts);
  },
};

export const av: String = "sqfdsfdsq";

const a = 10;

export let Am: Map<string, any> = new Map();

export const v = Math.random() + 80;

export const f = av[a];

export function* bfdsqfdsqfdsqf(): Generator<string, any, undefined> {
  return " "
};
export function* bfdsqf<T>(): Generator<string, any, undefined> {
  return " "
};

export class B { }

export abstract class Afdsqfdsqfdq<B> extends B { };
