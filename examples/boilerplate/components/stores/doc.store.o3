export store <DocStore
  private[string] page=loading...>
  <proto namespace="pages">
    case 'action:getPage':
      try {
        this.page = `/${ctx.route}.html`;
      } catch (error) {
        this.page = `${error.message}`;
        return '';
      }
      break;
  </proto>
</DocStore>
