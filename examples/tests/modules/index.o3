import what, { Test, SecondTest, isFunction } from './test.js';
import {
  someThing
} from './test.js';

<template>
  <p>user ${this.name}</p>
</template>

<proto type="app">
  def:
    name: null
  default:
    this.name = what;
    console.warn(what, { Test, SecondTest, isFunction });
</proto>