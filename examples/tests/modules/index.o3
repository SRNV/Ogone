import component random from '/examples/modules/module.js';
import * as test, defaulto from '/examples/modules/allAS.js';
import { lol, instance } from '/examples/modules/allAS.js';

<template>
  <p>user ${this.name}</p>
</template>

<proto>
  def:
    name: null
  default:
    this.name = random;
    console.warn(test, defaulto, instance, lol);
</proto>