<proto>
  def:
    count: 0
    counts:
      - 1
      - 2
      - 3
  case 'click:increase':
    const { i, c } = ctx;
    console.warn(ctx);
    this.counts[i] = c+1;
</proto>
<template>
  <div
    --for="counts as (c, i)"
    --click:increase> click ${c}</div>
</template>
<style>
  div {
    cursor: pointer;
    user-select: none;
  }
</style>