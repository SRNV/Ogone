<template>
  <style>
    @charset 'utf8';
    @import component url('fdsfdsqfdsqf') ;
    @export const* test = purple;
    @const* test2 = purple;
    @export const rule = .aaa div + other {
      color: $test;
      color: @color;
      & span {
        color: red;
      }
    }
    @keyframes aa {
      animation-name: $test;
    }
    div.container {
      ...$rule;
      ...$rule;
      ...$rule;
      ...$rule;
      ...$rule;
      ...$rule;
      ...$rule;
    }
  </style>
</template>