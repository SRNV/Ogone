import component Reuse from './reuse.o3';
<template>
  <Reuse />
</template>
<proto type="component">
  declare:
    count = 0;
    reusable = class extends HTMLElement {
      constructor(opts: any) {
        super();
        console.warn(opts);
      }
      attributeChangedCallback(prop: any, value: any) {
        console.warn(prop, value);
      }
    }
  default:
    setInterval(() => this.count++, 100);
    customElements.define('other-component', this.reusable);
    break;
</proto>