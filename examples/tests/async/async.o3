<proto type="async">
  def:
    user: null
  default:
    setTimeout(() => {
      // should trigger the then case of parent
    }, 0);
      Async.resolve(true);
  break;
</proto>

<img --await src="public/ogone.svg" width="200" />