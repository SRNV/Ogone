export enum Messages {
    GLOBAL_HELP = `
    HELP:
    ogone ...
        - run ...
            <PATH TO YOUR COMPONENT>.o3
        - build (todo)
        - create (todo)
        - fmt (todo)
        - lint (todo)
    `
}