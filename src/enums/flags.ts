export enum Flags {
    /**
     * adds the dev tool to the application
     */
    DEVTOOL = '--devtool',
    /**
     * shows logs from ogone
     */
    TRACE = '--ogone-trace',
    /**
     * let the user see his components
     * while developing it
     */
    DESIGNER = '--open-designer',
    /**
     * shows the last release messages
     */
    RELEASE = '--release',
}