use @/newapp/tests/async/async.o3 as 'promise'
<div>test</div>
<promise --then:act --catch:err --defer="promise">
  <div>...loading</div>
</promise>
<proto>
  def:
    promise: null
  case 'init':
    console.warn('iniiiit');
    this.promise = new Promise((resolve) => {
      setTimeout(() => {
        console.warn('resolveeeee parent')
        resolve(true);
      }, 200);
    });
  break;
  case 'then:act':
    console.warn('resolved', ctx);
  break;
  case 'catch:err':
    console.warn('catched err', ctx);
  break;
</proto>