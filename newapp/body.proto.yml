articles:
  - title: Experimental tool for a fullstack use.
    text: "Ogone is an experimental tool for Fullstack dev. \nFully based on Deno."
    code: "
        o3.serve({\n
      \tentrypoint: '/app/index.o3',\n
      \tport: 4000,\n
        })"

  - title: Get Started with the classic greeting app
    text: "in the file app/index.o3 follow this example and \n type deno run command into your terminal"
    code: "<p>Hello ${name}</p>\n
        <proto>\n
        \tdef:\n
        \t\tname: Rudy\n
        </proto>"

  - title: Install Ogone
    text: "this is how to install it"
    code: 'import o3 from "https://deno.land/x/ogone/mod.ts"'

  - title: Using def, case, default keywords to rule the component
    text: Todo
    code: "
      <proto>\n
      \tdef:\n
      \t\tyaml: true\n
      \tcase 'init':\n
      \tcase 'update:yaml':\n
      \tdefault:\n
      \t\tthis.yaml = true;\n
      \tbreak;\n
      </proto>"

  - title: read def in yml files
    text: Todo
    code: '<proto def="path/to/source.yml"/>'

  - title: Default/Cases execution
    text: Todo
    code: "
      <p>${count}</p>\n
      <button --click:increase>\n
      \tincrease\n
      </button>\n
      <proto>\n
      \tdef:\n
      \t\tcount:0\n
      \tcase 'init':\n
      \t\tthis.count = 0;\n
      \tbreak;\n
      \tcase 'click:increase':\n
      \t\tthis.count++;\n
      \t\tif (this.count > 5) execute case 'init';\n
      \tbreak;\n
      </proto>"